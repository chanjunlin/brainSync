import Message from '../message';
export default class Oracle {
    name: string;
    private systemPrompt;
    private modelName;
    private temperature;
    constructor(name: string, systemPrompt: string, modelName: string, temperature: number);
    ask(messages: Message[], question?: string): Promise<string | undefined>;
}
