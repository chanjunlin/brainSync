import InteractionHistory from '../interaction-history';
export type FileUpdate = {
    file: string;
    content: string;
};
export default class FilePatchService {
    history: InteractionHistory;
    modelName: string;
    temperature: number;
    constructor(history: InteractionHistory, modelName: string, temperature: number);
    generatePatch(fileUpdate: FileUpdate): Promise<string | undefined>;
}
