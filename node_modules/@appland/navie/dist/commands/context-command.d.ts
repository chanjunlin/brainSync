import Command, { CommandRequest } from '../command';
import LookupContextService from '../services/lookup-context-service';
import VectorTermsService from '../services/vector-terms-service';
import { ChatHistory } from '../navie';
import { ExplainOptions } from './explain-command';
export default class ContextCommand implements Command {
    private readonly options;
    private readonly vectorTermsService;
    private readonly lookupContextService;
    constructor(options: ExplainOptions, vectorTermsService: VectorTermsService, lookupContextService: LookupContextService);
    execute(request: CommandRequest, chatHistory?: ChatHistory): AsyncIterable<string>;
}
